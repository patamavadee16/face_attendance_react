import React, { useState} from 'react';
import Sidebar from '../components/Sidebar';
const Each = () => {
   
  
  
    return (
        <Sidebar>
        <div>
         Log out
        </div>
        </Sidebar>
    );
};

export default Each;